# Type of node front or wn
kube_type_of_node: front

kube_front_end_ip: 0.0.0.0
kube_domain: cloud.infn.it
dns_name: "{{ kube_front_end_ip }}.{{ kube_domain }}" 

# Deploy software
kube_deploy_dashboard: true
kube_deploy_monitoring: true
kube_deploy_cert_manager: true

node_name: AUTO
nodelabel: ''

# Software versions
kube_install_helm: true
helm_version: v3.18.6
cert_manager_helm_version: v1.18.2
cert_manager_type: letsencrypt-prod # [self-signed, letsencrypt-staging, letsencrypt-prod]
cert_manager_email: '' # used only for letsencrypt 
dashboard_helm_version: 7.13.0
monitoring_helm_version: 76.4.0
nerdctl_version: 2.1.3

# If true, running kubernetes etcd in ramdisk
etcd_in_RAM: true

# Properties related to GPU support
enable_gpu: false
gpu_support_role_name: ansible-role-gpu-support
gpu_support_role: "git+https://baltig.infn.it/infn-cloud/{{ gpu_support_role_name }},v1.2.5"
gpu_support_role_enable_install: false